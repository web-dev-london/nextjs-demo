'use client'
import React from 'react'

const AddCard = () => {
  return (
    <button
      className='btn btn-primary'
      onClick={() => console.log('click')}
    >Add Card</button>
  )
}

export default AddCard;